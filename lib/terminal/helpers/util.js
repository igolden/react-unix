/**
 * Filter JS
 */
module.exports._filterConfig(configKey) {
  // filters object for existent key,
  // else throws exception
  // this.config.keys.map((k, configKey) => {
  //   k === configKey ? true : false 
  // })
}

// handle '..', '...' paths
