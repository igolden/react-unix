/*
* The component that accepts users
* input from the interace.
* 
* @param  {} 
*/
class CommandLineInput {
  constructor(props) {
    //super(props)
    this.state = {
      stdin: ""
    }
  }
  render() {
    return ()
    // <input onChange={this.setState((s) => {cmds: s})}>
  }
}
