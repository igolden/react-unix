"use strict";

class Console {
  constructor() {
    // aliases?
  }
  /*
  * Send a command to the console
  * 
  * @param cmd {String} 
  * @param interpreter {Interpreter} 
  */
  send (cmd, interpreter) {
    // let command = sanitized(cmd)
    // send(command, interpreter)
  }

  /*
  * Clears the console scree and brings the
  * command line input to the top.
  */
  clear() {

  }


  _sanitized (string) {
    // returns string 
  }

  render() {
    return {
      //<UserInput  />
      //<History />
    }
  }
}

module.exports = Console;

