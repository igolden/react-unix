"use strict";

// const filter = require('').filterC

class Shell {
  constructor(options) {
    this.config = options;
  }
  /*
  * Sets a config value on the shell
  * 
  * @param key {String} 
  * @param val {Sting} 
  */
  setConfig(key, val) {
    // if this.config.filter(_filter(key))
  }
}
module.exports = Shell;
