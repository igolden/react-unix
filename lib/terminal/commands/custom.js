class Command {
  constructor(title, options) {
    this.config = options;
    this.type = type;
  }
  /*
  * MUST have the exec function to be
  * loaded as a custom command
  *
  * Need to find a way to get the stdin
  * args passed to it.
  * 
  * @param  {} 
  */
  exec () {
  }
}


// commmand = new Command("ls", {})
